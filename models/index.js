const { Contact, joiContactSchema, statusSchema } = require('./contact')
const { User, joiUserSchema } = require('./user')

module.exports = {
  Contact,
  joiContactSchema,
  statusSchema,
  User,
  joiUserSchema
}
